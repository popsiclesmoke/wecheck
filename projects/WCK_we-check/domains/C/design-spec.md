# [APPROVED] Design Spec — AUTH Authentication

---

## 문서 정보

| 항목 | 내용 |
|------|------|
| 프로젝트 코드 | WCK |
| 서비스명 | We-Check |
| 도메인 코드 | AUTH |
| 도메인명 | authentication |
| 작성자 | 강다현 (쿠콘 AX TF팀) |
| 작성일 | 2026-05-29 |
| 상태 | APPROVED |

> 기반 문서: `product-spec.md`, `feature-list.md`, `screen-list.md`
> 디자인 가이드: `projects/WCK_we-check/design/design-guide.md`

---

## 공통 레이아웃 규칙

> 모든 화면에 예외 없이 적용한다. (출처: `wecheck-spacing-spec.md`)

| 항목 | 값 |
|------|----|
| 화면 너비 | `360px` 고정 |
| 좌우 여백 (Page Margin) | `20px` 고정 |
| 주요 컴포넌트 너비 | `320px` (Button / Input / Dropdown 등) |
| 헤더 합산 높이 | `94px` = `StatusBar`(44px) + `MainHeader`(50px) |
| 콘텐츠 영역 시작점 | `top: 94px`, `padding: 0 20px`, `overflow-y: auto` |
| 하단 버튼 영역 | `padding: 0 20px 20px` — **하단 20px 고정 필수** (single Button 320px / double 155px×2 + gap 10px) |
| 폰 프레임 높이 | `680px` (미리보기 기준, `--screen-height` 토큰) |

> ⚠️ **공통 확인 사항 — 하단 버튼 영역 padding-bottom 필수**
> 버튼이 들어가는 하단 영역(`.bottom`)은 반드시 `padding-bottom: 20px(var(--page-margin))`을 고정 적용한다.
> 버튼 자체가 화면 최하단에 바로 닿지 않도록 아래 여백을 보장해야 한다.
> - single 버튼: `padding: 0 var(--page-margin) var(--page-margin)`
> - double 버튼: `padding: 0 var(--page-margin) var(--page-margin)` (각 버튼 155px, gap 10px)
> - 바텀시트 버튼: `padding: var(--space-md) var(--page-margin) var(--page-margin)` (상단 10px 허용)

**공통 컴포넌트 사용 규칙**
- 상단 상태바: 모든 화면 `<StatusBar />` (360×44px)
- 메인 헤더: `<MainHeader status="default | back | close" />` (360×50px)
- 바텀시트 헤더: `<BottomSheetHeader size="default | small" txtStyle="default | middle" />`
- 단일 CTA: `<Button layout="single" variant="primary | disabled" />`
- 2버튼 세트: `<Button layout="double" position="left | right" />`
- 팝업 하단 버튼: `<Button layout="popup" />` (290px, 하단만 radius 8px)

**Z-Index 레이어 (오버레이/팝업 포함 화면)**

| z-index | 요소 |
|---------|------|
| 1 | 딤 배경 (`top: 94px`부터) |
| 2 | `StatusBar` + `MainHeader` |
| 3 | 1차 바텀시트 |
| 4 / 5 | 2차 딤 / 2차 바텀시트 |
| 20 | 오버레이 팝업 (`OverlayPopup`, 290px 중앙 정렬) |

---

## 화면 설계

---

### AUTH-001 인증 방식 선택

**파일명:** `C00.인증방식선택.html`

**진입 조건**
- TERM-001 약관 동의 완료 후 진입

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | 뒤로가기 + 로고 |
| 본문-타이틀 | 화면 타이틀 영역 (`30px 20px 40px`) | "본인인증 방식을 선택해주세요" (`display1` 22px) |
| 본문-목록 | 인증 방식 선택 목록 (카드형 또는 `Radio` 리스트) | 각 항목 320px, radius 8px, 리스트 아이템 최소 62px, 로고 ↔ 텍스트 gap 14px. 항목 사이 gap 20px |
| 선택지 (3종) | ① 간편인증 로그인 ② 브라우저 인증서 ③ 금융인증서 | C00 화면 기준 확정. 각 카드: 좌측 primary accent bar + 타이틀 + 설명. 안내 "총 3번의 인증을 진행합니다". ※ 강다현 재확인 권장 |
| 하단 | `Button layout="single"` | 미선택 시 `disabled`, 선택 시 `primary` "다음" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 방식 선택 | — | 카드 테두리 선택 강조(`#2B5AFE` 2px), 하단 버튼 활성화 |
| "다음" 탭 | 간편인증서 선택 | AUTH-002 간편인증서 선택으로 이동 |
| "다음" 탭 | 휴대폰 인증 선택 | AUTH-007 휴대폰 인증 통신사 선택으로 이동 |
| "다음" 탭 | 미선택 | 비활성 (동작 안 함) |
| 뒤로가기 | — | TERM-001 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 인증 방식 목록 조회 실패 | "인증 방식을 불러오지 못했습니다. 다시 시도해주세요." |

---

### AUTH-002 간편인증서 선택

**파일명:** `C10.간편인증서_인증서선택.html`

**진입 조건**
- AUTH-001에서 "간편인증서" 선택 후 진입 (기본 레이아웃)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | 뒤로가기 + 로고 |
| 본문-타이틀 | 화면 타이틀 영역 | "인증서를 선택해주세요" (`display1` 22px) |
| 본문-그리드 | 간편인증서 카드 그리드 (2열) | 각 카드: 로고 + 인증서명(`heading5` 14px). radius 8px, 카드 내부 gap 12px. 그리드 행 사이 gap 20px |
| 카드 예시 | 카카오 / 네이버 / PASS / 페이코 / KB 등 | 카드 탭 시 인증서 종류 확정 |
| 하단 | (선택 즉시 진행 시 버튼 생략 가능) 또는 `Button layout="single"` | 정책에 따라 카드 탭 즉시 다음 단계 진행 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 카드 탭 (PASS) | — | AUTH-005 로딩 후 AUTH-006 PASS 통신사 선택 팝업 |
| 카드 탭 (그 외 인증서) | — | AUTH-005 로딩 후 AUTH-008 인증 요청 |
| 진입 직후 | 목록 조회 중 | AUTH-005 로딩 화면 노출 |
| 뒤로가기 | — | AUTH-001 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 인증서 목록 조회 실패 | "인증서 목록을 불러오지 못했습니다. 다시 시도해주세요." |

---

### AUTH-003 간편인증서 선택 (장차법)

**파일명:** `C10_1.간편인증서_인증서선택_장차법P.html`

**진입 조건**
- AUTH-001에서 간편인증서 선택 + 접근성(장애인차별금지법) 대응 모드일 때 (AUTH-002 변형)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문-타이틀 | 화면 타이틀 영역 | "인증서를 선택해주세요" — 텍스트 크기 확대 적용 |
| 본문-목록 | 간편인증서 목록 (접근성 변형) | 텍스트 크기·행 간격 확대, 탭 영역 확대. 가독성 우선 레이아웃(1열 또는 큰 카드). 로고 + 인증서명(`heading4` 15px 이상) |
| 하단 | `Button layout="single"` | 필요 시. 기본은 카드 탭 즉시 진행 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 카드 탭 | PASS / 그 외 | AUTH-002과 동일 분기 (PASS→AUTH-006, 그 외→AUTH-008) |
| 뒤로가기 | — | AUTH-001 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 목록 조회 실패 | "인증서 목록을 불러오지 못했습니다. 다시 시도해주세요." |

> 비고: 노출 조건 확정(2026-06-10) — **위탁기관(도입 기업) 설정 시** 장차법 접근성 레이아웃 기본 노출. ※ 강다현 재확인 권장

---

### AUTH-004 간편인증서 선택 (1열)

**파일명:** `C10_2.간편인증서_인증서선택_1열.html`

**진입 조건**
- AUTH-001에서 간편인증서 선택 + 1열 레이아웃 정책일 때 (AUTH-002 변형)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문-타이틀 | 화면 타이틀 영역 | "인증서를 선택해주세요" |
| 본문-목록 | 간편인증서 1열 리스트 | 각 행 320px, 로고(원형 40px) + 인증서명(`heading5`) 가로 배치, 로고↔텍스트 gap 14px. 리스트 아이템 최소 62px, 행 padding `11px 0` |
| 하단 | (카드 탭 즉시 진행) | — |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 행 탭 | PASS / 그 외 | AUTH-002과 동일 분기 |
| 뒤로가기 | — | AUTH-001 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 목록 조회 실패 | "인증서 목록을 불러오지 못했습니다. 다시 시도해주세요." |

---

### AUTH-005 간편인증서 목록 로딩

**파일명:** `C11.간편인증서_인증서선택_로딩.html`

**진입 조건**
- AUTH-002~004에서 인증서 목록 조회 중 / 인증서 선택 후 처리 중

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문 | 로딩 스피너 + 안내 텍스트 | 화면 중앙 정렬. 스피너 하단 안내 "잠시만 기다려주세요" (`body2` 14px), 스피너↔텍스트 gap 24px |
| 하단 | 없음 | — |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 로딩 완료 | 목록 조회 | AUTH-002~004 표시 |
| 로딩 완료 | 인증서 선택 후 | PASS→AUTH-006 / 그 외→AUTH-008 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 로딩 타임아웃 | "요청 시간이 초과되었습니다. 다시 시도해주세요." (이전 화면 복귀) |
| 네트워크 오류 | `Illust name="network"` + "네트워크 연결을 확인해주세요." |

---

### AUTH-006 PASS 통신사 선택 (팝업)

**파일명:** `C11_1.간편인증서_PASS통신사선택_팝업.html`

**진입 조건**
- AUTH-002~004에서 PASS 인증서 선택 시 (바텀시트 팝업)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 헤더(고정) | `StatusBar` + `MainHeader` | z-index: 2 |
| 딤 배경 | `top: 94px`부터 | z-index: 1 |
| 시트 헤더 | `BottomSheetHeader size="default" txtStyle="default"` | 좌: 타이틀 "통신사 선택" + 우: X |
| 본문 | `CarrierButton` 4개 (SKT / KT / LGU+ / 알뜰폰) | 각 77×46px, radius 8px. 선택 시 테두리 `#2B5AFE` 2px. 가로 나열, 본문 padding `0 20px 10px` |
| 하단 | `Button layout="double"` | 좌: "취소"(`position="left"`), 우: "확인"(`position="right" variant="primary"`). 각 155px + gap 10px |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 통신사 선택 | — | 해당 `CarrierButton` 선택 강조 |
| "확인" 탭 | 통신사 선택됨 | 시트 닫고 AUTH-008 인증 요청으로 이동 |
| "확인" 탭 | 미선택 | 동작 안 함 (선택 유도) |
| "취소" 탭 / X | — | 시트 닫고 인증서 선택(AUTH-002~004) 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 통신사 미선택 후 확인 | "통신사를 선택해주세요." |

---

### AUTH-007 휴대폰 인증 통신사 선택

**파일명:** `C11_2.간편인증서_휴대폰인증통신사선택.html`

**진입 조건**
- AUTH-001에서 휴대폰 인증 선택 시 (전용 전체 화면)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | 뒤로가기 + 로고 |
| 본문-타이틀 | 화면 타이틀 영역 (`30px 20px 40px`) | "통신사를 선택해주세요" (`display1` 22px) |
| 본문 | `CarrierButton` 4개 (SKT / KT / LGU+ / 알뜰폰) | 각 77×46px, 가로 나열. 선택 시 테두리 `#2B5AFE` 2px |
| 하단 | `Button layout="single"` | 미선택 `disabled`, 선택 시 `primary` "다음" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 통신사 선택 | — | 선택 강조, 하단 버튼 활성화 |
| "다음" 탭 | 선택됨 | AUTH-008 인증 요청으로 이동 |
| "다음" 탭 | 미선택 | 비활성 |
| 뒤로가기 | — | AUTH-001 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 통신사 미선택 | 버튼 disabled 유지 |

---

### AUTH-008 인증 요청

**파일명:** `C12_0.간편인증서_인증서선택_인증요청.html`

**진입 조건**
- AUTH-006(PASS 통신사 확인) 또는 AUTH-007(휴대폰 인증 통신사 선택) 후 진입 (인증 요청 전 상태)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | 뒤로가기 + 로고 |
| 본문-일러스트 | `Illust name="phoneLock"` | 44×61px, 중앙 정렬 |
| 본문-안내 | 안내 텍스트 | 일러스트↔텍스트 gap 24px. "인증 앱에서 인증을 진행해주세요" (`heading4` 15px) + 보조 안내(`body2` 14px) |
| 하단 | `Button layout="single" variant="primary"` | "인증 요청" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "인증 요청" 탭 | 정상 | 인증 앱 호출 + AUTH-009 대기 화면으로 전환 |
| 뒤로가기 | — | 이전 통신사 선택 화면 복귀 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 인증 요청 실패 | "인증 요청에 실패했습니다. 다시 시도해주세요." |
| 세션 만료 | AUTH-014 세션 만료 화면으로 이동 |

---

### AUTH-009 인증 요청 (대기 중)

**파일명:** `C12.간편인증_인증서선택_인증요청.html`

**진입 조건**
- AUTH-008에서 인증 요청 후 인증 앱 응답 대기 상태

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문-일러스트 | `Illust name="phoneLock"` (또는 `phoneChat`) | 중앙 정렬 |
| 본문-안내 | 대기 안내 + 타이머/로딩 | "인증 요청을 보냈습니다. 인증 앱에서 인증을 완료해주세요" + 남은 시간 타이머(`heading4`) |
| 본문-보조링크 | "알림을 받지 못하셨나요?" 텍스트 링크 | AUTH-012 알림 미수신 팝업 호출 |
| 하단 | `Button layout="single" variant="primary"` | "인증 완료" 또는 자동 폴링 시 대기 안내 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 인증 성공 (폴링/콜백) | 보안문자 방식 | AUTH-010 보안문자 입력으로 이동 |
| 인증 성공 (폴링/콜백) | 인증번호 방식 | AUTH-011 인증번호 입력으로 이동 |
| 인증 성공 (추가입력 불필요) | — | AUTH-013 인증 완료로 이동 |
| "알림 미수신" 링크 탭 | — | AUTH-012 팝업 노출 |
| 타이머 만료 | — | "인증 시간이 만료되었습니다." 후 AUTH-008 재요청 또는 AUTH-014 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 인증 거절/실패 | "인증이 완료되지 않았습니다. 다시 시도해주세요." |
| 타이머 만료 | "인증 시간이 만료되었습니다. 다시 요청해주세요." |
| 세션 만료 | AUTH-014로 이동 |

---

### AUTH-010 보안문자 입력

**파일명:** `C40.캡챠이미지.html`

**진입 조건**
- AUTH-009 대기 중 보안문자 입력이 요구되는 방식일 때

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문-타이틀 | 화면 타이틀 영역 | "보안문자를 입력해주세요" (`display1` 22px) |
| 본문-이미지 | 보안문자 이미지 + 새로고침(`Icon name="refresh"`) | 이미지 우측 새로고침 아이콘 |
| 본문-입력 | `Input state="default"` | 320×46px, placeholder "보안문자 입력". 폼 필드 gap 16px |
| 하단 | `Button layout="single"` | 미입력 `disabled`, 입력 시 `primary` "확인" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 입력 | — | 포커스 시 `Input state="focused"`, 하단 버튼 활성화 |
| 새로고침 탭 | — | 보안문자 이미지 재발급 |
| "확인" 탭 | 일치 | AUTH-013 인증 완료로 이동 |
| "확인" 탭 | 불일치 | `Input state="error"` + 에러 메시지 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 보안문자 불일치 | `Input state="error"` "보안문자가 일치하지 않습니다." (`caption2` 12px) |
| 미입력 후 확인 | 버튼 disabled 유지 |
| 세션 만료 | AUTH-014로 이동 |

---

### AUTH-011 인증번호 입력

**파일명:** `C12_3.간편인증서_인증요청_인증번호입력.html`

**진입 조건**
- AUTH-009 대기 중 인증번호(SMS 등) 입력이 요구되는 방식일 때

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="back"` | — |
| 본문-타이틀 | 화면 타이틀 영역 | "인증번호를 입력해주세요" (`display1` 22px) |
| 본문-입력 | `VerificationInput phase="sent"` | 입력칸 + 타이머(파란색, 예 `03:00`) + "재요청" 버튼(파란색) |
| 하단 | `Button layout="single"` | 미입력 `disabled`, 입력 시 `primary` "확인" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 입력 | — | 하단 버튼 활성화 |
| "재요청" 탭 | — | 인증번호 재발송, 타이머 리셋(`03:00`) |
| "확인" 탭 | 일치 | AUTH-013 인증 완료로 이동 |
| "확인" 탭 | 불일치 | 에러 메시지 노출 |
| 타이머 만료 | — | 입력 비활성, "재요청" 유도 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 인증번호 불일치 | "인증번호가 일치하지 않습니다." (`caption2` 12px) |
| 타이머 만료 | "인증 시간이 만료되었습니다. 재요청해주세요." |
| 미입력 후 확인 | 버튼 disabled 유지 |
| 세션 만료 | AUTH-014로 이동 |

---

### AUTH-012 알림 미수신 팝업

**파일명:** `T12.간편인증서_알림미수신팝업.html`

**진입 조건**
- AUTH-009에서 "알림을 받지 못하셨나요?" 링크 탭 시 (오버레이 팝업)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 배경 | 딤 배경 | z-index: 1 / 팝업 z-index: 20 |
| 헤더(고정) | `StatusBar` + `MainHeader` | z-index: 2 |
| 팝업 | `OverlayPopup` (290px 중앙) | radius 8px, 본문 padding `28px 20px 20px` |
| 팝업-제목 | 텍스트 `heading4` (15px) | "알림을 받지 못하셨나요?" |
| 팝업-내용 | 텍스트 `body2` (14px) | "인증 앱 알림을 확인하거나 인증을 재요청해주세요." |
| 팝업-하단 | `Button layout="double"` (팝업 내 2버튼) | 좌: "취소"(`position="left"`) / 우: "재요청"(`position="right" variant="primary"`) |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "취소" 탭 | — | 팝업 닫고 AUTH-009 대기 화면 복귀 |
| "재요청" 탭 | — | 인증 재요청 + AUTH-008/017로 재진입 (타이머 리셋) |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 재요청 실패 | "재요청에 실패했습니다. 잠시 후 다시 시도해주세요." |

---

### AUTH-013 인증 완료

**파일명:** `E00.인증완료.html`

**진입 조건**
- AUTH-010(보안문자) 또는 AUTH-011(인증번호) 인증 성공 후, 또는 AUTH-009 인증 성공 후 진입

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="default"` | 로고만 (back/close 없음) |
| 본문-일러스트 | `Illust name="phoneVerified"` | 44×61px, 중앙 정렬 |
| 본문-안내 | 완료 텍스트 | 일러스트↔텍스트 gap 24px. "본인인증이 완료되었습니다" (`display1` 22px) + 보조 안내(`body2` 14px) |
| 하단 | `Button layout="single" variant="primary"` | "다음" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "다음" 탭 | 정상 | ISSU 도메인(서류 발급)으로 진입 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 후행 도메인 진입 실패 | "다음 단계로 이동할 수 없습니다. 다시 시도해주세요." |

---

### AUTH-014 세션 만료

**파일명:** `E15.세션만료.html`

**진입 조건**
- 인증 진행 전 구간에서 세션이 만료된 경우 진입

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 헤더 | `MainHeader status="default"` | 로고만 |
| 본문-아이콘 | `Icon name="caution"` (또는 `error`) | caution 59×51px (`#FFCE3C`) / error 56×56px (`#FF4949`), 중앙 정렬 |
| 본문-안내 | 안내 메시지 | 아이콘↔텍스트 gap 24px. "세션이 만료되었습니다" (`display1` 22px) + "보안을 위해 처음부터 다시 진행해주세요." (`body2` 14px) |
| 하단 | `Button layout="single" variant="primary"` | "처음으로" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "처음으로" 탭 | 정상 | MAIN-001 위체크 메인으로 재시작 |

**오류 처리**

| 케이스 | 메시지 |
|--------|--------|
| 재시작 실패 | "다시 시도해주세요." (재시도 유도) |

---

### AUTH-015 간편인증 완료 (공통)

**파일명:** `C12.간편인증_인증서선택_인증완료(공통).html`

> ※ v0.5: scenario-list의 AUTH ID 체계(001~022)에 맞춰 환원. AUTH-009(인증 요청 대기 — 비활성)의 **완료 활성 상태**(인증 앱 응답 수신).

**진입 조건**
- AUTH-009에서 인증 앱 응답 완료 → 하단 버튼 활성화

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` + `MainHeader(back)` | — |
| 본문 | `Illust(phoneVerified)` + 안내 | AUTH-009와 동일 레이아웃, 완료 안내 |
| 하단 | `Button layout="single" variant="primary"` | "인증 완료" (활성) |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "인증 완료" 탭 | — | AUTH-013 인증 완료로 이동 |

---

### AUTH-016 인증번호 입력 (활성화)

**파일명:** `C12_3.간편인증서_인증요청_인증번호입력_활성화.html`

> ※ v0.5: scenario-list 체계 환원. AUTH-011(인증번호 입력)의 **입력 완료 활성 상태**.

**진입 조건**
- AUTH-011에서 인증번호 입력 완료 → 하단 버튼 활성화

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` + `MainHeader(back)` | — |
| 본문 | `VerificationInput(filled)` | AUTH-011과 동일, 인증번호 입력됨 |
| 하단 | `Button layout="single" variant="primary"` | "확인" (활성) |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| "확인" 탭 | 인증번호 정상 | AUTH-013 인증 완료로 이동 |

---

### AUTH-017 증명서 조회 로딩

**파일명:** `C20.증명서조회_로딩.html`

> ※ v0.4 신설 / v0.5 재번호(구 AUTH-015): 인증 후 제공기관별 증명서 조회 단계.

**진입 조건**
- AUTH-013 인증 완료 후, 제공기관별 증명서 조회 진행 중

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` + 제공기관 타이틀 | 좌측 원형 프로그래스 + 제공기관명(예: 정부24) |
| 본문 | 트럭 일러스트 + 프로그레스바 + 퍼센트 | 조회 진행률 %(첫 조회 7%부터 시작). 조회 중 증명서명 표시 |
| 하단 | 없음 | 자동 진행 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 조회 완료 | 해당 기관 증명서 조회 끝 | AUTH-018 조회 완료 노출 후 자동 이동 |
| 추가 조회 | 다른 기관 증명서 남음 | 인증방식/인증서선택(AUTH-001~) 재진입(인증 반복) |

---

### AUTH-018 증명서 조회 완료

**파일명:** `C22.증명서조회_완료.html`

> ※ v0.4 신설 / v0.5 재번호(구 AUTH-017).

**진입 조건**
- 제공기관 증명서 조회 완료 시

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 본문 | 트럭 일러스트 + 완료 텍스트 | "증명서 조회를 완료했습니다 / 다음 증명서로 자동이동합니다" |
| 하단 | 없음 | 자동 이동 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 자동 이동 | 추가 조회 있음 | 다음 기관 인증/조회로 |
| 자동 이동 | 모든 조회 완료 | ISSU/결과 도메인으로 |

---

### AUTH-019 증명서 조회 완료 (비회원)

**파일명:** `C22_비회원.증명서조회_완료.html`

> ※ v0.4 신설 / v0.5 재번호(구 AUTH-018).

**진입 조건**
- 비회원 발급 경로의 증명서 조회 완료 시

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 본문 | `circleCheck` + 서류 일러스트 + 완료 텍스트 | 비회원 조회 완료 안내 |
| 하단 | 없음 | 자동 이동 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 자동 이동 | — | 다음 단계로 |

---

### AUTH-020 증명서 조회 로딩 (진행도)

**파일명:** `C30.증명서조회_로딩.html`

> ※ v0.4 신설 / v0.5 재번호(구 AUTH-016): 일러스트형 간략 로딩.

**진입 조건**
- 증명서 조회 진행 중 (일러스트형 간략 로딩)

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` | — |
| 본문 | 일러스트 + 안내 텍스트 | "○○ 조회 중입니다" (예: 주민등록표등본 조회 중) |
| 하단 | 없음 | 자동 진행 |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 조회 완료 | — | AUTH-018 조회 완료로 이동 |

---

### AUTH-021 보안문자 — 음성듣기

**파일명:** `C40.캡챠이미지_음성듣기.html`

> ※ v0.5: scenario-list 체계 환원. AUTH-010(보안문자 입력)의 **음성듣기 상태**.

**진입 조건**
- AUTH-010 보안문자 화면에서 음성듣기(접근성) 선택 시

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` + `MainHeader(back)` | — |
| 본문 | 보안문자 이미지 + `Icon(sound/refresh)` + `Input` | AUTH-010과 동일, 음성듣기 활성 |
| 하단 | `Button layout="single"` | "확인" |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 음성 재생 | — | 보안문자 음성 안내 |
| "확인" 탭 | 정상 | 인증 진행 |

---

### AUTH-022 보안문자 입력 실패

**파일명:** `C40.캡챠이미지_입력실패.html`

> ※ v0.5: scenario-list 체계 환원. AUTH-010(보안문자 입력)의 **입력 실패(오류) 상태**.

**진입 조건**
- AUTH-010 보안문자 입력 값이 틀린 경우

**화면 구성**

| 영역 | 컴포넌트 | 설명 |
|------|----------|------|
| 상단 | `StatusBar` + `MainHeader(back)` | — |
| 본문 | 보안문자 이미지 + `Input` + 오류 안내 | "보안문자가 일치하지 않습니다" 등 인라인 오류 |
| 하단 | `Button layout="single"` | "확인" (재입력) |

**액션 및 분기**

| 액션 | 조건 | 결과 |
|------|------|------|
| 재입력 후 "확인" | 정상 | 인증 진행 |
| 새로고침 | — | 보안문자 이미지 재발급 |

---

## 컴포넌트 사용 요약

| 화면 | 주요 컴포넌트 |
|------|--------------|
| AUTH-001 | `MainHeader(back)`, `Radio`/카드, `Button(single)` |
| AUTH-002~004 | `MainHeader(back)`, 인증서 카드/리스트, `Button(single)` |
| AUTH-005 | `MainHeader(back)`, 로딩 스피너, `Illust(network)` (오류 시) |
| AUTH-006 | `BottomSheetHeader(default)`, `CarrierButton`×4, `Button(double)` |
| AUTH-007 | `MainHeader(back)`, `CarrierButton`×4, `Button(single)` |
| AUTH-008 | `MainHeader(back)`, `Illust(phoneLock)`, `Button(single)` |
| AUTH-009 | `MainHeader(back)`, `Illust(phoneLock/phoneChat)`, 타이머, `Button(single)` |
| AUTH-010 | `MainHeader(back)`, 보안문자 이미지, `Icon(refresh)`, `Input`, `Button(single)` |
| AUTH-011 | `MainHeader(back)`, `VerificationInput(sent)`, `Button(single)` |
| AUTH-012 | `OverlayPopup`, `Button(double)` |
| AUTH-013 | `MainHeader(default)`, `Illust(phoneVerified)`, `Button(single)` |
| AUTH-014 | `MainHeader(default)`, `Icon(caution/error)`, `Button(single)` |
| AUTH-015 | `MainHeader(back)`, `Illust(phoneVerified)`, `Button(single, primary)` (인증 완료 대기 — 활성) |
| AUTH-016 | `MainHeader(back)`, `VerificationInput(filled)`, `Button(single, primary)` (인증번호 입력 — 활성) |
| AUTH-017·020 | `StatusBar`, 프로그레스바/트럭·서류 `Illust`, 안내 텍스트 (증명서 조회 로딩) |
| AUTH-018~019 | `StatusBar`, 트럭/`circleCheck`/`Illust`, 완료 텍스트 (증명서 조회 완료) |
| AUTH-021~022 | `MainHeader(back)`, 보안문자 이미지, `Icon(sound/refresh)`, `Input` (보안문자 음성듣기/입력실패) |

---

## 미확정 사항

- ~~AUTH-001 인증 방식 선택지의 종류 및 수~~ → **확정**: 3종(간편인증 로그인/브라우저 인증서/금융인증서). C00 화면 기준.
- ~~AUTH-003 장차법 접근성 변형 노출 조건~~ → **확정**: 위탁기관 설정 시 노출.
- AUTH-002~004 인증서 카드 탭 시 즉시 진행 / 하단 버튼 진행 정책 확정 필요 *(미확정 — 강다현/디자이너 확정 대기)*

---

## Change History

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| v0.1 | 2026-05-29 | 강다현 | 최초 작성. AUTH-001~022 22개 화면 설계. WeCheck 컴포넌트/스페이싱 가이드 기반 |
| v0.4 | 2026-06-08 | AI (화면-문서 정합) | screen-list v0.2 정렬에 맞춰 전면 재정렬 — MAIN/TERM 중복(구 AUTH-001~008) 제거, 인증 화면 AUTH-001~014 재번호, 교차참조 정정(약관→TERM-001, 메인→MAIN-001), 보안문자 파일 C40으로 정정, 증명서조회 AUTH-015~018 신설. ※ 강다현 님 재확인 권장 |
| v0.5 | 2026-06-10 | AI (화면-문서 정합) | scenario-list.md(강다현 APPROVED, AUTH-001~022 체계)와의 Screen ID 충돌 해소 — AUTH-015~018(증명서조회 4종)을 **022 체계로 환원**: AUTH-015 간편인증 완료(공통, C12), AUTH-016 인증번호 입력 활성화(C12_3), AUTH-017 증명서조회 로딩(C20), AUTH-018 완료(C22), AUTH-019 완료 비회원(C22_비회원), AUTH-020 로딩 진행도(C30), AUTH-021 보안문자 음성듣기(C40), AUTH-022 보안문자 입력실패(C40). 컴포넌트 요약·교차참조 동기화. ※ 강다현 님 재확인 권장 |
| v0.6 | 2026-06-10 | AI (기획 확정) | 미확정 2건 확정 — AUTH-001 인증 방식 3종(간편인증 로그인/브라우저 인증서/금융인증서, C00 화면 기준), AUTH-003 장차법 변형 노출=위탁기관 설정 시. 미확정 사항 섹션 갱신. ※ 강다현 님 재확인 권장 |
